import { z } from "zod";

export const createRoomSchema = z.object({
    buyerId: z.string("L'identifiant de l'acheteur est requis"),
    sellerId: z.string("L'identifiant du vendeur est requis"),
});

export const updateRoomSchema = z.object({
    id: z.string("L'identifiant de la salle est requis"),
    buyerId: z.string("L'identifiant de l'acheteur est requis").optional(),
    sellerId: z.string("L'identifiant du vendeur est requis").optional(),
});

export const deleteRoomSchema = z.object({
    id: z.string("L'identifiant de la salle est requis"),
    buyerId: z.string("L'identifiant de l'acheteur est requis"),
    sellerId: z.string("L'identifiant du vendeur est requis"),
});