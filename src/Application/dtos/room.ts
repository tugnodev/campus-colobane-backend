export interface createRoomDto {
  buyerId: string;
  sellerId: string;
}

export interface updateRoomDto {
  id: string;
  buyerId: string;
  sellerId: string;
}

export interface deleteRoomDto {
  id: string;
  buyerId: string;
  sellerId: string;
}
